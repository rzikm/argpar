image: rikorose/gcc-cmake

stages:
  - build
  - test

build:
  stage: build
  script:
    - mkdir build
    - cd build
    - cmake ..
    - cmake --build .
  artifacts:
    paths:
      - build/bin
    
test:
  stage: test
  script:
    - ./build/bin/tests
